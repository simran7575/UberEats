class Card {
  constructor(id, url, title, description, rating, duration, units) {
    this.id = id;
    this.url = url;
    this.title = title;
    this.description = description;
    this.rating = rating;
    this.duration = duration;
    this.units = units;
  }
}

export default Card;
